# assets

무료 배포용 전자책 PDF를 이 폴더에 **`ebook.pdf`** 라는 이름으로 넣어 주세요.

- 원본: 《몸은 이미 말하고 있다》 — SVT 감각어휘 입문 가이드 (A5 18p)
- 이 폴더는 `public/` 이 아니므로 브라우저에서 직접 접근할 수 없습니다.
- 다운로드는 `/api/ebook?t=<서명토큰>` 을 통해서만 가능하며,
  토큰은 메일 주소를 남긴 사람에게만 발급되고 7일 후 만료됩니다.
